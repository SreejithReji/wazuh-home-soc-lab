# wazuh-certs-tool.sh config — sanitized from the real config used in this lab.
# Replace the IP below with your own indexer/manager/dashboard host's LAN IP
# (for a single-node deployment, all three node types share the same IP).

nodes:
  # Wazuh indexer nodes
  indexer:
    - name: node-1
      ip: "192.168.1.119"
    #- name: node-2
    #  ip: "<indexer-node-ip>"
    #- name: node-3
    #  ip: "<indexer-node-ip>"

  # Wazuh server nodes
  # If there is more than one Wazuh server
  # node, each one must have a node_type
  server:
    - name: wazuh-1
      ip: "192.168.1.119"
    #  node_type: master
    #- name: wazuh-2
    #  ip: "<wazuh-manager-ip>"
    #  node_type: worker
    #- name: wazuh-3
    #  ip: "<wazuh-manager-ip>"
    #  node_type: worker

  # Wazuh dashboard nodes
  dashboard:
    - name: dashboard
      ip: "192.168.1.119"
