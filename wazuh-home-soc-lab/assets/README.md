# Assets

Placeholder for diagrams/screenshots referenced from the main README or docs (e.g. dashboard screenshots, network topology images). The primary architecture diagram is embedded directly in the root `README.md` as a Mermaid diagram, so this folder is currently optional/empty — add real screenshots here as the lab grows (e.g. `dashboard-overview.png`, `agent-list.png`, `health-check-pass.png`).
