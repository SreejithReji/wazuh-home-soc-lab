# Assets

Screenshots captured directly from this lab build, embedded throughout the README and docs.

| File | Description |
|---|---|
| `dashboard-overview.jpg` | Wazuh Overview page — Agents Summary + Last 24 Hours Alerts severity breakdown |
| `agent-detail.jpg` | `sree-pi-lab` agent detail page — status, MITRE ATT&CK top tactics, PCI DSS compliance breakdown |
| `nmap-scan-terminal.jpg` | Terminal output of the Nmap reconnaissance scan against the Pi |
| `hydra-attack-terminal.jpg` | Terminal output of the Hydra SSH brute-force attempt |
| `alert-discover-view.jpg` | Wazuh Discover view — raw `wazuh-alerts-*` index showing decoded alert fields including MITRE mapping |

The primary architecture diagram is a Mermaid diagram embedded directly in the root `README.md` (renders natively on GitHub), so it doesn't need an image file here.
